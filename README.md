# PYQ Backend

### Setup

Install all packages:

```
npm install
```

### Start Server (Dev mode)

```
npm run dev
```

### Format all files

Run this before staging files (before using `git add` ):

```
npm run format
```

and then commit.
